const initUpdateNavbarOnScroll = () => {
    const navbar = document.querySelector('.red');
      navbar.addEventListener('click', () => {
        console.log(event);
        });
    }

export { initUpdateNavbarOnScroll };